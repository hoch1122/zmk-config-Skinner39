---
include:
  # 2026-06-12: v0.3 + 左central構成（右central化はrevert済み）
  # HWMv1 のためボード名にサフィックスなし。Studioスニペットなし恒久運用。CI: @v0.3
  - board: skinner39_left
    shield: nice_view
  - board: skinner39_right
    shield: nice_view
  # 2026-06-12: central役入れ替えで左右間bondが矛盾し通信不能になったため追加。
  # settings_reset は左用1つで両半身に使える（同一MCU・同一パーティション。settings消去のみのため）。
  # 右用は trackball ノードと最小構成が衝突しビルド不可なので作らない
  - board: skinner39_left
    shield: settings_reset
